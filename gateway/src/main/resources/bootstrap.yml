spring:
  application:
    name: gateway
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config-server
      name: client
      fail-fast: true
      label: master
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/