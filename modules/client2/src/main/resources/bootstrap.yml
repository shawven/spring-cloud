spring:
  application:
    name: client
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      label: master
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/