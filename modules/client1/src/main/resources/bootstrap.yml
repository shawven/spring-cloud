spring:
  application:
    name: client
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      label: master
      profile: dev

eureka:
  client:
    service-url:
      defaultZone: http://${eureka.address}:8001/eureka/
